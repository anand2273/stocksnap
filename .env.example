BOT_TOKEN=replace-with-your-botfather-token
APP_MODE=polling

# Required only when APP_MODE=webhook outside Render:
# WEBHOOK_BASE_URL=https://your-public-domain.example
# WEBHOOK_SECRET=replace-with-a-random-secret
